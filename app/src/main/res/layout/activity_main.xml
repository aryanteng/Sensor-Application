<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_feedback_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:textSize="14sp"
        />

    <ToggleButton
        android:id="@+id/proximityToggleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textOff="Start Proximity Data Collection"
        android:textOn="Stop Proximity Data Collection" />

    <ToggleButton
        android:id="@+id/lightToggleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="20dp"
        android:textOff="Start Light Data Collection"
        android:textOn="Stop Light Data Collection" />

    <ToggleButton
        android:id="@+id/geomagneticRotationVectorToggleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textOff="Start Geomagnetic Data Collection"
        android:textOn="Stop Geomagnetic Data Collection" />

</LinearLayout>
